# 成都旅游智能体 · 面试不足分析 + 生产差距评估 + 最终锐评

> 基于「LlamaIndex RAG + LangGraph 手写 + PostgresSaver」改造后的方案，做一次不留情面的全面体检。

---

## 一、面试不足分析（面试官可能追问的薄弱点）

### 1.1 技术深度层面的不足

| 薄弱点 | 具体问题 | 面试官可能的追问 | 风险等级 |
|--------|---------|----------------|---------|
| **LlamaIndex 深度不够** | 只用到了 VectorStoreIndex + HybridRetriever + Reranker 三件套，没有涉及 LlamaIndex 的高级特性（如 Query Transform、Sub-Question Query Engine、Router Query Engine） | "LlamaIndex 的 Retriever 和 QueryEngine 有什么区别？你为什么不用 Sub-Question Query Engine 拆解复杂问题？" | ⚠️ 中 |
| **LangGraph 手写但状态机简单** | 只有 Supervisor → Worker → Supervisor 的线性路由，没有用到 LangGraph 的高级特性（如 Map-Reduce、人工中断 interrupt、并行节点 send） | "LangGraph 的 `send` API 用过吗？你的图里如果有多个 Worker 可以并行执行，你怎么设计？" | ⚠️ 中 |
| **RAG 评测体系薄弱** | 只有 20 条自建 Query 的 Top1 召回率，没有 recall@k、precision、MRR、NDCG 等标准指标，也没有对抗样本测试 | "你的 RAG 评测集是怎么构造的？有没有测过 Query 改写、错别字、口语化表达的召回率？" | ⚠️ 高 |
| **Agent 评测缺失** | 没有 Agent 端到端的自动化评测（如用 LLM-as-Judge 评分行程质量），只有手动 Case 验证 | "你怎么评估 Agent 生成的行程规划质量？有没有自动化评测体系？" | ⚠️ 高 |
| **PostgresSaver 理解停留在 API 层** | 能讲 setup/get_tuple/put，但对内部三表的写入时序、checkpoint_id 的乐观锁机制、channel_versions 的 CAS 原理理解不深 | "PostgresSaver 的 put 方法里怎么处理并发写入冲突？checkpoint_id 和 channel_versions 是什么关系？" | ⚠️ 中 |

### 1.2 项目"真实性"层面的不足

| 薄弱点 | 具体问题 | 面试官可能的追问 | 风险等级 |
|--------|---------|----------------|---------|
| **数据规模太小** | 370 条向量、35 个景点，这个规模下任何向量库都能跑出 100% 召回 | "如果数据量扩大到 10 万条，你的 RAG 架构要怎么调整？索引、分片、检索延迟怎么优化？" | ⚠️ 高 |
| **硬规则覆盖窄** | 只有 8 条规则，且 validate_plan 只强制了 R-001（熊猫 Day1 上午）一条 | "R-002 到 R-008 你是怎么程序级强制的？如果 LLM 违反了 R-005（春熙路傍晚），你的校验逻辑怎么 catch？" | ⚠️ 高 |
| **LTM 抽取质量不可验证** | 用 LLM 抽取用户画像到 MySQL 三槽位，但没有评测抽取准确率 | "你怎么保证 LTM 抽取的'不吃辣'是准确的？有没有误抽取或漏抽取的情况？" | ⚠️ 中 |
| **缺乏真实用户测试** | 所有 Case 都是自己设计的，没有真实用户反馈数据 | "这个项目有真实用户用过吗？用户反馈怎么样？" | ⚠️ 中 |

### 1.3 工程化层面的不足

| 薄弱点 | 具体问题 | 风险等级 |
|--------|---------|---------|
| **没有 CI/CD** | GitHub 仓库只有代码，没有 GitHub Actions 跑测试、Lint | ⚠️ 中 |
| **没有 Docker Compose** | 虽然有 Dockerfile，但没有 docker-compose.yml 一键起 MySQL + Redis + Postgres + 后端 + 前端 | ⚠️ 中 |
| **没有监控告警** | LangSmith 追踪有，但没有 Prometheus metrics + Grafana 面板 + 告警规则 | ⚠️ 中 |
| **测试覆盖率低** | pytest ≥15 条，但没有覆盖率报告，关键路径（硬规则校验、记忆注入）的覆盖率未知 | ⚠️ 中 |
| **没有 API 版本管理** | `/api/chat` 没有版本号，接口变更没有向后兼容策略 | ⚠️ 低 |

### 1.4 面试话术层面的建议（主动暴露短板）

不要试图掩盖以上不足，面试时主动说：

> "这个项目在 RAG 评测和 Agent 端到端评测上还比较薄弱，目前只有 20 条自建 Query 的召回率测试。如果时间允许，我计划接入 LangSmith 的在线评测功能，用 LLM-as-Judge 对行程质量做自动化评分，并扩充评测集到 100 条以上，加入错别字、口语化、多轮上下文等对抗样本。"

主动暴露 = 有自我认知 = 加分。

---

## 二、生产落地差距评估

### 2.1 差距全景图

| 维度 | 当前 Demo 状态 | 生产级要求 | 差距 | 修复难度 |
|------|--------------|-----------|------|---------|
| **并发能力** | 单用户 Demo，SqliteSaver/PostgresSaver 单连接 | 100+ QPS，连接池 + 负载均衡 | 🔴 大 | 中 |
| **高可用** | 单点部署，无容灾 | 多实例 + 健康检查 + 自动重启 | 🔴 大 | 高 |
| **数据安全** | API Key 明文存 .env | 密钥管理服务（KMS/Vault）+ 加密传输 | 🔴 大 | 中 |
| **限流降级** | 文档提了 Token Bucket 但未实现 | 全局限流 + 单用户限流 + LLM 超时降级 | 🟡 中 | 中 |
| **监控告警** | 只有 LangSmith trace | Prometheus + Grafana + 告警（错误率/延迟/Token 用量） | 🟡 中 | 中 |
| **日志体系** | Loguru 文件日志 | 结构化日志（JSON）+ ELK/Loki 聚合 | 🟡 中 | 低 |
| **CI/CD** | 无 | GitHub Actions + 自动测试 + 自动部署 | 🟡 中 | 低 |
| **测试覆盖** | ≥15 条用例，无覆盖率 | 单元测试 ≥80% + 集成测试 + E2E 测试 | 🟡 中 | 中 |
| **部署方式** | 本地双击启动 | Docker Compose / K8s + 蓝绿部署 | 🟡 中 | 高 |
| **成本控制** | 无 Token 预算管理 | 按用户/按 API 的 Token 配额 + 预算告警 | 🟡 中 | 低 |
| **LLM 容错** | 单次调用，无重试 | 重试 + 回退模型（DeepSeek 挂了切备用） | 🟡 中 | 低 |
| **数据质量** | 35 景点手工 CSV | 数据校验 + 增量更新 + 质量监控 | 🟢 小 | 低 |

### 2.2 核心差距详解

#### 差距 1：并发能力（🔴 最大差距）

**当前问题：**
- PostgresSaver 默认单连接，`from_conn_string` 创建的是单连接而非连接池
- FastAPI 单进程，没有 Gunicorn/Uvicorn workers
- 没有连接池配置，高并发下数据库连接耗尽

**生产级方案：**
```python
# PostgresSaver 支持 ConnectionPool
from psycopg_pool import ConnectionPool
from langgraph.checkpoint.postgres import PostgresSaver

pool = ConnectionPool(
    conninfo=settings.CHECKPOINT_POSTGRES_URI,
    min_size=5,
    max_size=20,
)
saver = PostgresSaver(pool)  # 传入连接池而非单连接
```
- FastAPI 用 `uvicorn workers=4` 或 Gunicorn + Uvicorn workers
- Nginx 反向代理 + 负载均衡

#### 差距 2：高可用（🔴）

**当前问题：** 单点部署，进程挂了服务就不可用。

**生产级方案：**
- 至少 2 个后端实例 + 负载均衡
- PostgreSQL 主从复制 + 自动故障转移
- Redis Sentinel/Cluster
- 健康检查端点 + K8s liveness/readiness probe

#### 差距 3：限流降级（🟡）

**当前问题：** 文档提了 Token Bucket 但代码里没实现。

**生产级方案：**
```python
# 全局限流（防止 LLM API 被刷爆）
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

@app.post("/api/chat")
@limiter.limit("10/minute")  # 每用户每分钟 10 次
async def chat(request: Request, ...):
    ...
    # LLM 调用超时降级
    try:
        result = await asyncio.wait_for(graph.ainvoke(...), timeout=30)
    except asyncio.TimeoutError:
        # 降级到规则引擎
        return fallback_rule_engine(user_input)
```

#### 差距 4：LLM 容错（🟡）

**当前问题：** 单次 LLM 调用，失败就崩。

**生产级方案：**
```python
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10))
async def llm_invoke(messages):
    return await llm.ainvoke(messages)

# 主模型失败回退到备用模型
try:
    response = await llm_invoke(messages)
except Exception:
    response = await backup_llm.ainvoke(messages)  # 如 Qwen / GLM
```

### 2.3 从 Demo 到生产的工作量估算

| 工作项 | 预估工时 | 优先级 |
|--------|---------|--------|
| 连接池 + 多 worker 部署 | 1-2 天 | P0 |
| 限流降级 + LLM 重试 | 1-2 天 | P0 |
| Docker Compose 一键部署 | 1 天 | P1 |
| CI/CD + 自动测试 | 1-2 天 | P1 |
| Prometheus 监控 + 告警 | 2-3 天 | P1 |
| 密钥管理（KMS/Vault） | 1-2 天 | P2 |
| 多实例高可用 + 负载均衡 | 2-3 天 | P2 |
| K8s 部署 + 蓝绿发布 | 3-5 天 | P3 |

**总计：约 2-3 周可达到"小规模生产可用"，1-2 个月达到"企业级生产就绪"。**

---

## 三、最终锐评

### 3.1 改造后的方案评分

| 维度 | 原方案（DeepAgents+手写RAG+SqliteSaver） | 改造后（LangGraph+LlamaIndex+PostgresSaver） | 提升 |
|------|--------------------------------------|-------------------------------------------|------|
| 面试表现力 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | +1（LlamaIndex + 手写 LangGraph + PG 更有深度） |
| 工程真实度 | ⭐⭐⭐ | ⭐⭐⭐⭐ | +1（LlamaIndex 成熟 RAG，PG 生产级持久化） |
| 技术深度 | ⭐⭐⭐ | ⭐⭐⭐⭐ | +1（手写 LangGraph 能讲底层机制） |
| 生产就绪度 | ⭐⭐ | ⭐⭐⭐ | +1（PostgresSaver 支持并发） |
| 可维护性 | ⭐⭐ | ⭐⭐⭐ | +1（摆脱 DeepAgents 不稳定 API） |

### 3.2 改造的核心价值

1. **从"脚手架拼装"升级为"理解底层"**：手写 LangGraph 让你能讲清 Checkpointer、State reducer、条件边路由，而不是只会调 `create_deep_agent()`。

2. **RAG 从"玩具级"升级为"工业级"**：LlamaIndex 的 HybridRetriever + Reranker 比手写 jieba 软重排专业一个量级，面试时能讲"为什么选 LlamaIndex"体现选型能力。

3. **持久化从"Demo 级"升级为"生产级"**：PostgresSaver 支持并发，SqliteSaver 只能单人用，这个差距在面试时一讲就懂。

### 3.3 仍然存在的硬伤（无法通过框架选型解决）

1. **数据规模太小**：370 条向量的 RAG 没有说服力。**建议：** 爬取马蜂窝/携程的成都景点数据，扩充到 1000+ 条向量，或者用 LLM 生成更多景点描述。

2. **评测体系缺失**：只有 Top1 召回率，没有完整的 RAG 评测和 Agent 评测。**建议：** 接入 LangSmith 在线评测，用 LLM-as-Judge 评分。

3. **硬规则覆盖窄**：只强制了 R-001 一条。**建议：** 把 R-001~R-008 全部实现程序级校验（距离校验、时间校验、预算校验）。

4. **没有真实用户反馈**：所有 Case 都是自导自演。**建议：** 发布到公网（用 Supabase + Vercel），收集 10+ 真实用户反馈。

### 3.4 一句话终极锐评

> **改造后的方案，框架选型已经从"面试能说"升级到"面试能扛住追问"，但项目本身仍然是一个"精致的 Demo"——技术栈齐全、架构完整、关键词拉满，但数据规模、评测体系、工程化深度离生产级项目还有 2-3 周的工作量。**
>
> **面试策略：主动展示框架选型的思考过程（对比 DeepAgents/LangGraph、对比手写 RAG/LlamaIndex、对比 SqliteSaver/PostgresSaver），用"选型能力"弥补"数据规模"的不足；同时主动承认评测和工程化的短板，并给出具体的改进计划。**
>
> **如果只能再优化一件事：扩充数据规模到 1000+ 条向量，并接入 LangSmith 在线评测。这两件事做完，项目就从"简历项目"变成"有说服力的项目"。**

---

## 四、面试前最后 Checklist

- [ ] 能画出 LangGraph 手写架构图（Supervisor + 4 Worker + 条件边）
- [ ] 能讲清 PostgresSaver 三表模型 + 上下文管理器生命周期坑
- [ ] 能讲清 LlamaIndex HybridRetriever + Reranker 的工作原理
- [ ] 能回答"为什么不选 DeepAgents / 手写 RAG / SqliteSaver"
- [ ] 能主动承认数据规模小、评测体系弱的不足，并给出改进计划
- [ ] 本地 PostgreSQL 装好了，PostgresSaver 能跑通
- [ ] LlamaIndex 的 RAG 能跑通，Top1 召回率 100% 能复现
